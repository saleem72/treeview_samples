//

import 'package:flutter/material.dart';

class NeomorphisimDarkScreen extends StatelessWidget {
  const NeomorphisimDarkScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Neomorphis Dark'),
      ),
      body: _buildWidgets(context),
    );
  }

  Widget _buildWidgets(BuildContext context) {
    return Container();
  }
}
